Always use chinese to answer questions.

- Avoid using any as much as possible. If any must be used, you must report it
  (including where it is used and why), or prefer using unknown instead.
- Avoid type assertions (as) as much as possible. Prefer using zod for runtime
  type validation instead.
