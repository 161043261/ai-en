# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

# psql -U root -W
# create databse ai_en;
# pnpm exec prisma migrate dev --name init
DATABASE_URL="postgresql://root:pass@127.0.0.1:5432/ai_en"

# jwt secret key
JWT_SECRET="012345abcdefghijklmnopqrstuvwxyz"

# minio access key
MINIO_ACCESS_KEY="Z12BNSVSNTDCMBKP52TQ"

# minio secret key
MINIO_SECRET_KEY="UPBN8pgzPLcAzcl2zwCXpjqA5GMhAbsKrOFV9eWK"

# minio endpoint
MINIO_ENDPOINT="127.0.0.1"

# minio port
MINIO_PORT=9000

# minio use ssl
MINIO_USE_SSL=0

# minio bucket
MINIO_BUCKET="bucket"


# Added `myminio` successfully.

# Access Key: Z12BNSVSNTDCMBKP52TQ

# Secret Key: UPBN8pgzPLcAzcl2zwCXpjqA5GMhAbsKrOFV9eWK

# Expiration: no-expiry
